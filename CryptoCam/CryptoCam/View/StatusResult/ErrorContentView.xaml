<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CryptoCam.Views.StatusResult.ErrorContentView">
  <ContentView.Content>
        <StackLayout Grid.Row="0" Orientation="Vertical" BackgroundColor="Transparent">
            <Image Source="{Binding errorIconSource}" 
               WidthRequest="144"
               HeightRequest="144"
               Margin="0,100,0,0"               
               />

            <Label Margin="30" FontSize="Medium" Text="{Binding LongDescription}"
                VerticalOptions="Start" 
                HorizontalOptions="CenterAndExpand" />
            <Label FontSize="50"
                   Margin="50"
                   Text="{Binding ShortDescription}"
                VerticalOptions="StartAndExpand" 
                HorizontalOptions="CenterAndExpand" />






        </StackLayout>

    </ContentView.Content>
</ContentView>